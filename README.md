# Seven Knights Rebirth — GvG 3v3 Tracker

Next.js (App Router) + Tailwind + localStorage

## Run locally
```bash
npm install
npm run dev
```
Open http://localhost:3000

## Deploy to Vercel
- Push this folder to GitHub
- Go to vercel.com → New Project → Import the repo → Deploy
